<head> <link rel="stylesheet" href="/css/openP.css">
    <link rel="stylesheet" href="/css/style.css"></head>
<h1 class="welcomeLand">Welcome to {{lands.title}}</h1>
<p>{{lands.description}}</p>
<ul>
    {{#each rides as |ride|}}
    
    <li><a href="/ride/{{ride.land_id}}/{{ride.id}}">{{ride.title}}</a></li>
 
    {{/each}}
 </ul>
